version: "2"
services:
  harvester:
    build: .
    environment:
      VIRTUAL_HOST: harvester.docker
    ports:
      - '80'
    volumes:
      - './data/:/data/'
      - './src:/harvester/src'
      - './vendor:/harvester/vendor'
